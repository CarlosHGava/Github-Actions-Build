name: "Build iOS app"

on:
  workflow_dispatch:
    branches: [main]

jobs:
  build_with_signing:
    runs-on: self-hosted
    steps:
      - name: Verificando a versão do Xcode 
        run: /usr/bin/xcodebuild -version

      - name: Fazendo o checkout do repositório
        uses: actions/checkout@v4

      - name: Gerando a build
        run: |
          xcodebuild clean build test \
            -project Github\ Actions\ Build.xcodeproj \
            -destination 'platform=iOS Simulator,name=iPhone 16,id=03943A75-BECC-4676-B3EE-4CE2CEFFD4A5,OS=18.3.1' \
          clean archive
